/*** SmartFoxServer 2X Examples - SimpleChat* http://www.smartfoxserver.com* (c) 2010 gotoAndPlay*/package {		import com.smartfoxserver.v2.SmartFox;	import com.smartfoxserver.v2.SFSEvent;		public class ConnectionTest {				private var sfs:SmartFox;				/**		 * Application initialization.		 * An instance of the SmartFoxServer client API is created, and event listeners are added.		 */		public function ConnectionTest():void		{			sfs = new SmartFox(true);						// Add event listeners			sfs.addEventListener(SFSEvent.CONNECTION, onConnection);		}				/**		 * On successful connection established, login user.		 */		private function onConnection(evt:SFSEvent):void {			if (evt.params.success)				trace("connection!");			else {				trace("failed connection...");				// Set login panel state				loginPanel.isConnecting = false;								// Show message				loginPanel.ta_error.text = "Unable to connect to " + sfs.currentIp + ":" + sfs.currentPort + "\nPlease check the client configuration";			}		}	}}